import java.awt.Color;

import graphics.MazeCanvas;

public class EntryCell extends EdgeCell {

    public EntryCell(MazeCanvas canvas, int row, int col, Color color) {
        super(canvas, row, col, color);
    }
}
